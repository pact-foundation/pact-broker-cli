asyncapi: "2.6.0"
info:
  title: Fraud Events API
  version: "1.0.0"
channels:
  fraud/alert:
    subscribe:
      operationId: receiveFraudAlert
      message:
        payload:
          type: object
          properties:
            transactionId:
              type: string
            riskScore:
              type: number
